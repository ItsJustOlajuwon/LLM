# Samuel V2 — Medium Model (~26M parameters)
# Good for CPU training or single GPU

vocab_size: 32000
d_model: 384
n_heads: 6
n_layers: 8
d_ff: 1024
max_seq_len: 512
dropout: 0.1
pos_encoding: rotary
norm_type: rmsnorm
norm_eps: 1.0e-06
activation: swiglu
tie_embeddings: true
bias: false
